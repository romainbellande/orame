datasource db {
    provider = "postgres"
    url      = env("DATABASE_URL")
}

generator client {
    provider      = "cargo prisma"
    output        = "../src/lib.rs"
}

model User {
    id          String  @id @default(cuid())
    name        String
    email       String  @unique
    password    String
}

